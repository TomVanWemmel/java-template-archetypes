# Persistence module configuration
# Add your database and persistence configuration here

# Database configuration
# db.url=jdbc:your-database-url
# db.driver=your.database.Driver
# db.username=username
# db.password=password

# ORM configuration (if using an ORM framework)
# orm.dialect=your.orm.Dialect
# orm.auto=update
# orm.show_sql=true
# orm.format_sql=true

# Connection pool configuration (if using a connection pool)
# pool.maximumPoolSize=10
# pool.minimumIdle=5
# pool.idleTimeout=30000
